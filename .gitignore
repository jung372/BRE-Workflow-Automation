# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# 로그
*.log
monitor.log
logs/

# 실행 락 / 스모크 격리 디렉터리 (run_local.py 가 생성)
run.lock
deploy.lock
_smoke/

# 서버 배포 산출물 (InstallRoot 는 저장소 밖이지만 실수 방지용)
releases/
launcher/
current_release.txt
deployment.json
release.json

# 민감 설정 (이메일 계정 등)
config.json
.env

# 압축 파일
*.zip

# 로컬 전용 앱 (Flask 서버 / 배치 파일은 GitHub에 올려도 무방하나 선택적)
# dashboard_app.py
# run_dashboard.bat
# run_korec_monitor.bat
